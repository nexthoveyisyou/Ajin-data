# =================================================
# 			Pandas : 필터링과 자료형 변환
# =================================================

# [사용 파일]
# - students_records.json

# [학습 범위]
# - 불린 인덱싱
# - isin()
# - query()
# - astype()
# - pd.to_numeric()
# - pd.to_datetime()

# [ 문제 유형 ]
# - 문제 1~10은 필터링 복습 문제입니다.
# - 문제 11~20은 자료형 변환 복습 문제입니다.
# - 문제 20은 자료형 변환과 필터링을 함께 복습하는 종합 문제입니다.

# #########################################################
# #				필터링 문제 10개                                              
# #########################################################
# =================================================
# 문제 1. score가 90점 이상인 학생만 출력하세요.
# =================================================
import pandas as pd
df = pd.read_json(r'./DATA/students_records.json')

print("문제1")
print(df[df['score']>=90])
print("-"*20)

# =================================================
# 문제 2. age가 21세 이상인 학생만 출력하세요.
# =================================================
print("문제2")
print(df[df['age']>=21])
print("-"*20)

# =================================================
# 문제 3. gender가 "여"인 학생만 출력하세요.
# =================================================
print("문제3")
print(df[df['gender']=='여'])
print("-"*20)

# =================================================
# 문제 4. major가 "데이터분석"인 학생만 출력하세요.
# =================================================
print("문제4")
print(df[df['major']=='데이터분석'])
print("-"*20)

# =================================================
# 문제 5. score가 80점 이상이고 gender가 "여"인 학생만 출력하세요.
# =================================================
print("문제5")
mask1 = (df.score >=80) & (df.gender =='여')
print(df.loc[mask1])
print("-"*20)

# =================================================
# 문제 6. age가 21세 이상이고 score가 90점 이상인 학생의 name, age, score 
#           컬럼만 출력하세요.
# =================================================
print("문제6")
mask1 = (df.age >=21) & (df.score >= 90)
print(df.loc[mask1,['name', 'age', 'score']])
print("-"*20)

# =================================================
# 문제 7. isin()을 사용하여 major가 "데이터분석" 또는 "인공지능"인 학생만 
#           출력하세요.
# =================================================
print("문제7")
isin_filter = df['major'].isin(['데이터분석', '인공지능'])
print(df[isin_filter])
print("-"*20)

# =================================================
# 문제 8. isin()을 사용하여 name이 "김민수", "최유진", "정다은" 중 하나인 
#           학생만 출력하세요.
# =================================================
print("문제8")
isin_filter = df['name'].isin(["김민수", "최유진", "정다은"])
print(df[isin_filter])
print("-"*20)

# =================================================
# 문제 9. query()를 사용하여 score가 90점 이상인 학생만 출력하세요.
# =================================================
print("문제9")
print(df.query('score >= 90'))
print("-"*20)


# =================================================
# 문제 10. query()를 사용하여 age가 21세 이상이고 score가 85점 이상인 
#             학생만 출력하세요.
# =================================================
print("문제10")
print(df.query('age >= 21 & score >= 85'))
print("-"*20)

# #########################################################
# #				자료형 변환 문제 10개                                       
# #########################################################
# =================================================
# 문제 11. 각 컬럼의 자료형을 확인하세요.
# =================================================
print("문제11")
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 12. id 컬럼의 자료형을 문자열형(str)으로 변환한 후 자료형을 확인하세요.
# =================================================
print("문제12")
df['id'] = df['id'].astype("str")
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 13.  age 컬럼의 자료형을 int8로 변환한 후 자료형을 확인하세요.
# =================================================
print("문제13")
df['age'] = df['age'].astype("int8")
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 14. score 컬럼의 자료형을 float으로 변환한 후 자료형을 확인하세요.
# =================================================
print("문제14")
df['score'] = df['score'].astype("float")
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 15.  score 컬럼을 astype(str)을 사용하여 문자열형으로 변환하세요.
#              그 후 자료형을 확인하세요.
# =================================================
print("문제15")
df['score'] = df['score'].astype("str")
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 16.  문자열형으로 변환된 score 컬럼을 pd.to_numeric()을 사용하여 
#               다시 숫자형으로 변환하세요.
#               그 후 자료형을 확인하세요.
# =================================================
print("문제16")
df['score'] = pd.to_numeric(df['score'])
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 17. age 컬럼을 문자열형으로 변환한 후, pd.to_numeric()을 사용하여 
#             다시 숫자형으로 변환하세요.
# =================================================
print("문제17")
df['age'] = df['age'].astype("str")
df['age'] = pd.to_numeric(df['age'])
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 18. 다음 코드를 실행하여 score 컬럼에 잘못된 문자열 값을 추가하세요.
#              df.loc[5] = [6, "테스트", "남", 24, "컴퓨터공학", "점수없음", "2026-03-10"]
#              그 후 pd.to_numeric()의 errors="coerce"를 사용하여 숫자로 변환할 
#              수 없는 값을 NaN으로 처리하세요.
# =================================================
print("문제18")
df.loc[5] = [6, "테스트", "남", 24, "컴퓨터공학", "점수없음", "2026-03-10"]
df['score'] = pd.to_numeric(df['score'], errors="coerce")
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 19.  join_date 컬럼을 pd.to_datetime()을 사용하여 날짜형으로 변환하세요.
#            그 후 자료형을 확인하세요.
# =================================================
print("문제19")
df['join_date'] = pd.to_datetime(df['join_date'])
print(df.dtypes)
print("-"*20)

# =================================================
# 문제 20. join_date 컬럼을 날짜형으로 변환한 후, 가입일이 2026-03-05 이후인 
#            학생만 출력하세요.
# =================================================
print("문제20")
df['join_date'] = pd.to_datetime(df['join_date'])
print(df[df['join_date'] > '2026-03-05'])
print("-"*20)